@model IEnumerable<SuperERP.Vendas.DTO.EmpresaDTO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Incluir")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Alterar", new { empresaID = item.ID }) |    
            @Html.ActionLink("Delete", "Excluir", new { empresaID = item.ID })
        </td>
    </tr>
}

</table>
